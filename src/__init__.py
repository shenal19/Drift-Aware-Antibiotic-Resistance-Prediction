"""Drift-Aware Antibiotic Resistance Prediction — source package.

Makes `src` importable so modules can use `from src import ...` (scripts at the
project root) and `from . import ...` (within the package). Run modules as:
    python -m src.<module>        # e.g. python -m src.modeling
from the project root.
"""
